### @explicitHints true

# Always Daytime

## Complete the code

```blocks
loops.forever(function () {
    gameplay.timeSet(gameplay.time(MIDDAY))
    loops.pause(60000)
})
```

```template
loops.forever(function () {
    gameplay.timeSet(gameplay.time(MIDDAY))
})
```

## Try it!

Night will never fall again.